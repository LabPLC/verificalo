<%= form_tag({ controller: 'answers', action: 'search' },
    { method: 'post', id: 'form-answers' }) do %>
<div class="row">
  <div class="col-sm-9">
    <input name="answers_query" type="text" 
           placeholder="Hoy no circula, verificación, etc"
           <% if request.post? and params[:answers_query].present? %>
           value="<%= params[:answers_query] %>"
           <% end %>
           class="form-control input">
  </div>
  <div class="col-sm-3">
    <button id="btn-answers-search" type="submit"
            class="btn btn-primary btn">Buscar</button>
  </div>
</div>
<% end %>    
<p class="help-block">Ingrese los terminos de su pregunta y haga
  click en el botón buscar.</p>
