<%= render 'search' %>

<!-- answers#home -->
<div class="container">
  <div class="row">
    <div class="col-md-8">
      <% if @results && @results.count > 1 %>
      <%= render 'results' %>
      <% else %>
      <%= render 'categories' %>
      <% end %>
    </div>
    <div id="sidebar-answer" class="col-md-4">
      <%= render 'cta' %>
    </div>
  </div>
</div>
