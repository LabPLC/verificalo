<!-- notifier#weekend -->
<h1 style="color: #494b57;
           font-size: 30px; 
           font-weight: normal; 
           margin: 20px 0 20px 0;
           text-transform: uppercase;">Auto 
  <strong><%= @user.plate %></strong></h1>

<% if @vehicle.verificacion_current? %>
<p stlye="margin: 20px 0;">
  <%= @vehicle.verificacion_current_brand_str %> 
  <%= @vehicle.verificacion_current_model_str %> 
  <%= @vehicle.verificacion_current_year_str %>
</p>
<% end %>

<%= render 'no_circula_verificacion' %>

<%= render 'no_circula_weekend' %>

<%= render 'end' %>
