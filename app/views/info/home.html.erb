<!-- info#home -->
<div class="jumbotron">
  <div class="container">
    <div class="row">
      <div class="col-md-6 col-md-offset-3">
        <h1>Verifícalo</h1>
        <div class="row">
          <div class="col-sm-10 col-sm-offset-1 col-xs-12">
            <p>Ingresa la placa de tu auto y consulta el estado de la
              verificación, el programa hoy no circula, infracciones y
              tenencias.</p>
            <% if @error %>
            <!-- si hay error -->      
            <% if @error == 'INVALID_PLATE' %>
            <div class="alert alert-warning"><strong>La placa no es de
                un auto particular de la Ciudad de
                México.</strong></div>
            <% else %>
            <div class="alert alert-warning"><strong>Intenta más
                tarde:</strong> Tenemos problemas técnicos para
              consultar el estado de tu auto.</div>
            <% end %>
            <% end %>
          </div>
        </div>
        <%= form_tag({ controller: 'info', action: 'home' },
            { method: 'post', id: 'form-home' }) do %>
        <div class="row">
          <div class="col-sm-10 col-sm-offset-1 col-xs-12">
            <input name="plate" type="text" placeholder="Placa"
                   class="form-control input-lg text-center">
          </div>
        </div>
        <div class="row">
          <div class="col-sm-4 col-sm-offset-4 col-xs-12">
            <button type="submit" class="btn btn-primary btn-lg"
                    id="btn-home">Consulta</button>
          </div>
        </div>
        <% end %>
      </div>
    </div>
  </div>
</div>

<div class="container">
  <div class="row">
    <div class="col-md-4 col-md-offset-1">
      <h2>Recordatorios</h2>
      <p class="cta">Evita filas o multas recibiendo avisos oportunos
        sobre tus obligaciones como automovilista particular de la Ciudad
        de México.</p>
      <div class="row">
        <div class="col-xs-12">
          <a href="<%= url_for({ controller: 'notices',
                   action: 'home' })%>" id="btn-notices"
             class="btn btn-primary pull-right">¡Suscríbete!</a>
        </div>
      </div>
    </div>
    <div class="col-md-4 col-md-offset-2">
      <h2>Preguntas y respuestas</h2>
      <p class="cta">Encuentra verificentros, dónde pagar tus adeudos y
         todo lo que necesitas saber sobre una mejor movilidad en la
         Ciudad de México.</p>
      <div class="row">
        <div class="col-xs-12">      
          <a href="<%= url_for({ controller: 'answers',
                   action: 'home' })%>" id="btn-answers" 
             class="btn btn-primary pull-right" href="#">Preguntar</a>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- analytics -->
<script>
  ga('set', 'page', '/info/home');
  ga('set', 'title', 'Info - Home');
  ga('send', 'pageview');
</script>
