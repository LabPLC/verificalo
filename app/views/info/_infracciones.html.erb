<div class="row">
  <% if @vehicle.infracciones_unpaid > 0 %>
  <!-- con adeudos de infracciones -->
  <div class="col-xs-1 red">&#11044;</div>
  <div class="col-xs-11">
    <p>
      <% if @vehicle.infracciones_unpaid > 1 %>
      <strong>Tu auto tiene <%= @vehicle.infracciones_unpaid %>
        infracciones sin pagar.</strong>
      <% else %>
      <strong>Tu auto tiene una infracción sin pagar.</strong>
      <% end %>
    </p>
    <p>Recuerda que debes pagar tus infracciones antes de llevar tu
      auto a verificar.</p>
  </div>
  <% else %>
  <!-- sin adeudos de infracciones -->
  <div class="col-xs-1 green">&#11044;</div>
  <div class="col-xs-11">
    <p>Tu auto no tiene infracciones sin pagar.</p>
  </div>
  <% end %>
  
  <!-- links sobre infracciones -->    
  <div class="col-xs-12">
    <a href="<%= url_for({ controller: 'info', 
             action: 'infracciones',
             plate: @vehicle.plate })%>"
       class="btn btn-primary btn-sm btn-wide">Mi historial de
      infracciones</a>
  </div>
</div>
